[providers]
python = {}

[phases.setup]
nixPkgs = ["python3", "gcc"]

[phases.install]
cmds = ["python -m venv /cpv && /cpv/bin/pip install -r requirements.txt"]

[start]
cmd = "/cpv/bin/uvicorn main_chat:app --host 0.0.0.0 --port $PORT"