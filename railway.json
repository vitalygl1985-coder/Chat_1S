{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install"
  },
  "deploy": {
    "startCommand": "npx ts-node server.ts",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}